/**
 * Platform Experience — Branding configuration.
 */

import type { ExperienceBranding } from '@/app/experiences/types'

export const platformBranding: ExperienceBranding = {
  label: 'Core Platform',
  icon: 'mdi-domain',
  color: '#6200ea',
}
